<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules for Android 12+ (API 31+)
    See: https://developer.android.com/about/versions/12/backup-restore#xml-changes
-->
<data-extraction-rules>
    <!-- Cloud backup configuration (Google Drive backup) -->
    <cloud-backup>
        <!-- Include shared preferences for backup -->
        <include domain="sharedpref" path="." />

        <!-- Exclude connection settings (hostname/port) for security -->
        <exclude domain="sharedpref" path="ComfyChairPrefs.xml" />
    </cloud-backup>

    <!-- Device-to-device transfer configuration -->
    <device-transfer>
        <!-- Include all preferences for device transfer (more trusted) -->
        <include domain="sharedpref" path="." />
    </device-transfer>
</data-extraction-rules>
