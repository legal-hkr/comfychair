<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ComfyChair" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <!-- Splash screen theme (dark) -->
    <style name="Theme.ComfyChair.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:colorBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_comfychair_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ComfyChair</item>
    </style>
</resources>